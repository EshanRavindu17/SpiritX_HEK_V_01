// context/actions/modeActions.js

export const setMode = (mode) => ({
    type: 'SET_MODE',
    payload: mode,
  });
  
  export const setProfileVisibility = (visibility) => ({
    type: 'SET_PROFILE_VISIBILITY',
    payload: visibility,
  });
  
  
  